const shapes = require('./shapes.js');

// From Walter Perry, TA
// Tests that just came to my mind
// validate if the user answers the questions (3)
describe('shapes', () => {
  it('should validate if the user answers the questions', () => {
    expect(shapes).toBeDefined();
  });
});

// creating (instantiating) the classes (4)
describe('shapes', () => {
  it('should create 4 classes', () => {
    expect(shapes).toBeDefined();
  });
});

// calling the methods (depends on your code)
describe('shapes', () => {
  it('should call the methods', () => {
    expect(shapes).toBeDefined();
  });
});
// returning things from the methods (text color, shape, svg) (could be more)
describe('shapes', () => {
  it('should return the text color', () => {
    expect(shapes).toBeDefined();
  });
});
// writing the file (1)
describe('shapes', () => {
  it('should write the file', () => {
    expect(shapes).toBeDefined();
  });
});
// concatenating all the inputs of the SVG
describe('shapes', () => {
  it('should concatenate all the inputs of the SVG', () => {
    expect(shapes).toBeDefined();
  });
});